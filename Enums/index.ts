//An enum is a special "class" that represents a group of constants (unchangeable variables)
enum directions {
  North,
  West,
  East,
  South,
}

//String enums
enum direction {
  North = "sahil",
  West = "sahil",
  East = "sahil",
  South = "sahil",
}
enum random {
  North,
  West,
  East,
  South,
}
console.log(directions.North);
console.log(direction.West);
console.log(random.West);
